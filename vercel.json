{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/dist/*.js": { "maxDuration": 300 }
  },
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}